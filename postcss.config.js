/** @type {import('postcss-load-config').Config} */
export default {
  plugins: {
    // Tailwind v3：
    tailwindcss: {},
    autoprefixer: {},

    // Tailwind v4（如果你在用 v4）：
    // '@tailwindcss/postcss': {},
  },
};